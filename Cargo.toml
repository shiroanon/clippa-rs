[package]
name = "clippa-rs"
version = "0.1.4"
edition = "2024"

[dependencies]
# Standard clippa-rs needs
wl-clipboard-rs = "0.8"
notify-rust = "4.1"
regex = "1.10"
tokio = { version = "1", features = ["full", "signal"] }
dirs = "5.0"

# Manager needs
ratatui = "0.26"
crossterm = "0.27"
